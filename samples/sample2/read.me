The first time you run this program you should
run it without arguments from the command-line,
the program creates the a new DBase file.
Later call the program with the name of the created
DBase-file (test.dbf), eg.

   sample2 test.dbf